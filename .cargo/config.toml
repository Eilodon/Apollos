[target.aarch64-linux-android]
# Linker and AR should be provided via environment variables:
# CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER
# CARGO_TARGET_AARCH64_LINUX_ANDROID_AR
# Or rely on a configured cross-compilation environment.
